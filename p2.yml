---
- name: Deploy a jar file
  hosts: ribhu
  remote_user: dheer009
  tasks:
    - name: Copy jar file to home directory
      copy:
        src: ./target/calculatorDevOps-1.0-SNAPSHOT-jar-with-dependencies.jar
        dest: ~/
